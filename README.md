# Pixabay React Native Project

## 🚀 How to use

- Install packages with `npm install` to install dependencies.
- If there's a problem with code above, run `npm install legacy-peer-deps`.
- Run `npm start` to start the bundler.
